let tyupe=confirm("number tyupedagi son kiritasizmi?");
if (tyupe==true) {
    let num1=Number(prompt("1-sonni kiriting:"));
    let num2=Number(prompt("2-sonni kiriting:"));
    if (num1===num2) {
        console.log(true);
    }
    else{
        console.log(false);
    }
}
else{
    let num1=Number(prompt("1-sonni kiriting:"));
    let num2=Number(prompt("2-sonni kiriting:"));
    if (num1===num2) {
        console.log(true);
    }
    else{
        console.log(false);
    }
}
